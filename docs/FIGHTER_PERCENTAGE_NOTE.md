# Fighter Completion Estimate

After this helper coverage pass, Fighter is estimated at about 88% complete.

Remaining work is mostly:

- replace older local Fighter checks with shared helpers
- tidy duplicated combat-sheet Fighter summary logic
- improve visible builder choices
- do final pass testing and cleanup

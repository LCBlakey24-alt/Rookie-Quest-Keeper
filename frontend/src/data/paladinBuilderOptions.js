import { getPaladinProgressionSummary } from './paladinProgression';
import { getPaladinSubclassKey, getPaladinSubclassOptions } from './paladinSubclasses';
export const PALADIN_FIGHTING_STYLES=['Defense','Dueling','Great Weapon Fighting','Protection'];
export function getPaladinBuilderOptions(level=1, edition='2014'){const summary=getPaladinProgressionSummary(level,edition); const choices=summary.activeFeatures.filter(f=>f.type==='choice'); const needsSubclass=choices.some(c=>c.choiceType==='subclass'); const needsFightingStyle=choices.some(c=>c.choiceType==='fighting_style'); const weaponMasteryChoices=choices.filter(c=>c.choiceType==='weapon_mastery').reduce((n,c)=>Math.max(n,c.choices||0),0); return {...summary,choices,needsSubclass,needsFightingStyle,weaponMasteryChoices,subclassOptions:getPaladinSubclassOptions(summary.edition),fightingStyleOptions:PALADIN_FIGHTING_STYLES,requiredChoiceLabels:choices.map(c=>c.name),helperText:choices.map(c=>c.choiceType==='subclass'?'Choose a Paladin oath.':`Choose ${c.name}.`).join(' ')};}
export function isValidPaladinSubclass(value='',edition='2014'){const key=getPaladinSubclassKey(value); return getPaladinSubclassOptions(edition).some(o=>o.key===key||getPaladinSubclassKey(o.value)===key);}
export function validatePaladinBuilderSelections({level=1,edition='2014',subclass='',fightingStyle=''}={}){const options=getPaladinBuilderOptions(level,edition); const errors=[]; if(options.needsSubclass&&!isValidPaladinSubclass(subclass,edition))errors.push('Choose a Paladin oath.'); if(options.needsFightingStyle&&!fightingStyle)errors.push('Choose a Paladin fighting style.'); return {valid:errors.length===0,errors,options};}

export default function JoinCodeDialog({ invite, loading, onClose, onCopy, onRotate }) { return <div className="dashboard-modal-overlay" role="presentation"><section className="dashboard-modal-panel" role="dialog" aria-modal="true" aria-labelledby="join-code-title"><div className="dashboard-modal-header"><div><p className="dashboard-eyebrow">Player invite</p><h2 id="join-code-title">{invite.campaign_name || 'Campaign'} join code</h2></div><button type="button" onClick={onClose} disabled={loading} className="dashboard-close-button" aria-label="Close join code"><span>×</span></button></div><div className="dashboard-join-code">{invite.join_code || '------'}</div><p className="dashboard-muted">Give this 6-character code to players. They can use Join Campaign and select which character to link.</p><div className="dashboard-modal-actions"><button type="button" onClick={onClose} disabled={loading} className="unified-dashboard-button"><span>Close</span></button><button type="button" onClick={onRotate} disabled={loading} className="dashboard-small-button"><span>{loading ? 'Generating...' : 'Generate New Code'}</span></button><button type="button" onClick={onCopy} disabled={loading || !invite.join_code} className="dashboard-primary-button"><span>Copy Code</span></button></div></section></div>; }

{
  "name": "tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "scripts": {
    "install:browsers": "playwright install chromium",
    "install:browsers:deps": "playwright install --with-deps chromium",
    "check:screenshot": "node scripts/check-playwright-screenshot.mjs",
    "screenshots:site": "node scripts/capture-site-screenshots.mjs"
  }
}
